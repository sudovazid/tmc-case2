default:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: "data_warehouse_dev"
      threads: 1
      keyfile: "{{ env_var('GCP_SA_KEY') }}"
      location: US
